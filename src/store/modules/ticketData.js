const state = {
  bgColor: 'lightblue',
  fontSize: 2,
  fontColor: 'gold',
  passengerName: 'Max Mustermann',
};

const actions = {};

const mutations = {};

export default {
  state,
  actions,
  mutations,
};
